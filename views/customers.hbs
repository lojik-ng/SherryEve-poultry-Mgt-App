<div class="page-header">
    <div class="page-header-info">
        <h2>🛒 Customer Management</h2>
        <p>Manage your buyers and their orders</p>
    </div>
    <div class="page-header-actions">{{#canDo 'customers' 'create'}}<button class="btn btn-primary"
            data-modal="modalAddCustomer">+ Add
            Customer</button>{{/canDo}}</div>
</div>

<div class="card">
    <div class="card-body">
        {{#if customers.length}}
        <div class="table-container">
            <table class="data-table">
                <thead>
                    <tr>
                        <th>Name</th>
                        <th>Phone</th>
                        <th>Email</th>
                        <th>Address</th>
                        <th>Added</th>
                        <th>Actions</th>
                    </tr>
                </thead>
                <tbody>
                    {{#each customers}}
                    <tr>
                        <td><strong>{{this.name}}</strong></td>
                        <td>{{#if this.phone}}{{this.phone}}{{else}}—{{/if}}</td>
                        <td>{{#if this.email}}{{this.email}}{{else}}—{{/if}}</td>
                        <td>{{#if this.address}}{{truncate this.address 30}}{{else}}—{{/if}}</td>
                        <td>{{formatDate this.created_at}}</td>
                        <td class="table-actions">
                            {{#canDo 'customers' 'delete'}}
                            <form action="/customers/{{this.id}}/delete" method="POST" style="display:inline">
                                <button type="submit" class="btn btn-ghost btn-sm text-danger"
                                    data-confirm="Delete this customer?">🗑</button>
                            </form>
                            {{/canDo}}
                        </td>
                    </tr>
                    {{/each}}
                </tbody>
            </table>
        </div>
        {{else}}
        <div class="empty-state">
            <div class="empty-icon">🛒</div>
            <h3>No Customers</h3>
            <p>Add your first customer</p>
        </div>
        {{/if}}
    </div>
</div>

{{#canDo 'customers' 'create'}}
<div class="modal-overlay" id="modalAddCustomer">
    <div class="modal">
        <div class="modal-header">
            <h3>Add Customer</h3><button class="modal-close">✕</button>
        </div>
        <form action="/customers" method="POST" data-async>
            <div class="modal-body">
                <div class="form-group"><label>Name <span class="required">*</span></label><input type="text"
                        class="form-control" name="name" required></div>
                <div class="form-row">
                    <div class="form-group"><label>Phone</label><input type="text" class="form-control" name="phone">
                    </div>
                    <div class="form-group"><label>Email</label><input type="email" class="form-control" name="email">
                    </div>
                </div>
                <div class="form-group"><label>Address</label><textarea class="form-control" name="address"
                        rows="2"></textarea></div>
                <div class="form-group"><label>Notes</label><textarea class="form-control" name="notes"
                        rows="2"></textarea></div>
            </div>
            <div class="modal-footer"><button type="button" class="btn btn-outline"
                    data-dismiss="modal">Cancel</button><button type="submit" class="btn btn-primary">Add
                    Customer</button></div>
        </form>
    </div>
</div>
{{/canDo}}